# ── Core deep learning ────────────────────────────────────────────────────────
torch>=2.0.0
torchvision>=0.15.0
ultralytics>=8.0.0          # YOLOv8 / YOLOv10 + Ultralytics training API

# ── Computer vision & data processing ────────────────────────────────────────
opencv-python>=4.8.0        # image I/O, CLAHE, video writing, overlay drawing
albumentations>=1.3.0       # bbox-aware augmentation pipeline
numpy>=1.24.0
pillow>=10.0.0

# ── Agentic layer — LangChain + LLM backends ─────────────────────────────────
langchain>=0.2.0             # chain / prompt / LLMChain abstractions
langchain-community>=0.2.0   # Ollama integration (local LLM, fully offline)
langchain-openai>=0.1.0      # OpenAI ChatGPT integration (requires API key)

# ── MLOps & experiment tracking ───────────────────────────────────────────────
wandb>=0.17.0                # W&B run tracking, loss curves, ablation tables
                             # Usage: wandb login  (once)
                             #        export WANDB_PROJECT=aquathreat

# ── Utilities ────────────────────────────────────────────────────────────────
pyyaml>=6.0
tqdm>=4.65.0
scikit-learn>=1.3.0          # used in metrics helpers
matplotlib>=3.7.0            # local plot generation
